@model IEnumerable<S1G1_SISAPP.StudyTerm>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TermName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TermStartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TermEndDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TermYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeamSeason)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TermName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TermStartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TermEndDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TermYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamSeason)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TermID.Trim() }) |
            @Html.ActionLink("Details", "Details", new { id=item.TermID.Trim() }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TermID.Trim() })
        </td>
    </tr>
}

</table>
